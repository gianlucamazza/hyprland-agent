"""Voice subsystem for hyprland-agent.

Provides STT (faster-whisper), TTS (Piper), PTT (Unix socket trigger),
wake-word and VAD engines (future), the capture pipeline FSM,
and the voice integration for the daemon.
"""
